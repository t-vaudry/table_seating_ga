#include <iostream>
#include <time.h>

#include "functions.h"

using namespace std;

int main()
{
    srand(unsigned(time(NULL)));
    cout << "Hello World!" << endl;
    system("pause");
    return 0;
}